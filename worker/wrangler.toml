name = "synix-proxy-installer"
main = "install-redirect.js"
compatibility_date = "2024-01-01"

# Route: synix.dev/proxy → this worker
# Configure in CF dashboard or via:
#   routes = [{ pattern = "synix.dev/proxy", zone_name = "synix.dev" }]
